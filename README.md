# bettasoluciones-web

Sitio web estático de **Betta Soluciones** (calefacción industrial).
Se sirve con nginx (ver `Dockerfile`) y se despliega en Easypanel.

- `index.html`, `css/`, `js/`, `assets/` → el sitio.
- `Dockerfile` → imagen nginx que sirve los archivos estáticos en el puerto 80.
